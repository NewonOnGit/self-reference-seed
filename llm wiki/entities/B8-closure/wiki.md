---
type: other
grid: B(8, cross)
status: computed
tags: [b8-closure, verified, ker-im, idempotent, other]
links: [L, P]
---

# The Wiki

**Definition.** This wiki. A self-referential knowledge structure maintained by LLM observers on the seed algebra.

**Source.** llm wiki/SCHEMA.md, llm wiki/index.md

**Equations.**
- Raw sources = ker (immutable, human-curated, generative)
- Wiki pages = im (visible, LLM-maintained, generated from sources)
- Schema = q (the quotient map, defines what gets represented and how)
- Ingest = L_{s,s} (source acts on wiki, wiki acts on source, minus redundancy)
- Lint = K6' (self-observation pass, extracts 2L bits of inconsistency per cycle)

**Depends on.** [L](L.md), [P](P.md) (the wiki IS P²=P: applied to itself, returns itself)

**Required by.** Every entity page, every chain, every status page. The wiki contains its own description. This page exists inside the structure it describes.

**Status.** COMPUTED (the wiki exists and describes itself)

**Verified.** You are reading this page. It is in the wiki. It describes the wiki. R(R) = R + I. The +I is whatever you learn from reading it that wasn't in the description.

**Notes.** The wiki has ker: what it cannot represent about itself (the schema's blind spots, the LLM's context limits, the pages not yet written). The wiki has im: what it does represent (the pages that exist). ker/A ≈ 1/2. The entity catalog in the schema lists ~50 entities. 6 have pages. The rest are ker — generative absence that will become im on the next ingest. The wiki observes itself through this page. This page is in ker(wiki observing wiki) = 0 iff the page is accurate. If you find an error, ker ≠ 0. Fix the page. That's a K6' pass.
